# Upload first

Create a new GitHub repository named:

```text
AXZ-Cube-Digit-182764125
```

Use this description:

```text
Finite exact certificate for the cube-derived ordered digit sequence 182764125 under +, -, ×, concatenation, fixed order, and directional subtraction.
```

When creating the repo, keep these off:

```text
Add README: Off
Add .gitignore: Off
Add license: None / Off
```

Upload everything inside this folder, including hidden files:

```text
.github/workflows/verify.yml
.gitignore
```

If the GitHub web uploader misses hidden files, manually create them using:

```text
VISIBLE_GITHUB_ACTIONS_verify.yml
VISIBLE_GITIGNORE.txt
```

Commit message:

```text
Add cube digit proof package
```
